/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [],
  },
  experimental: {
    // Required for server actions if used in future
  },
};

export default nextConfig;
